# Dados de conexão Oracle
spring.datasource.url=jdbc:oracle:thin:@oracle.fiap.com.br:1521:ORCL
spring.datasource.username=rm558277
spring.datasource.password=fiap24

# Dialeto do Hibernate para Oracle
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.Oracle12cDialect
spring.jpa.database-platform=org.hibernate.dialect.Oracle12cDialect

# Estratégia de nomes
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Geração do esquema (deixe como 'none' se já tem o schema criado no banco)
spring.jpa.hibernate.ddl-auto=none

# Exibir SQL no console
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# (Opcional) Configuração para Flyway, se estiver usando
spring.flyway.baselineOnMigrate=true
